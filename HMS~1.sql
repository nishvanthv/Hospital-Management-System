CREATE TABLE APPOINTMENT (appointment_id NUMBER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,patient_id NUMBER NOT NULL,doctor_id NUMBER NOT NULL,appointment_date DATE NOT NULL,created_at DATE DEFAULT SYSDATE,CONSTRAINT fk_patient FOREIGN KEY (patient_id)REFERENCES PATIENT(patient_id),CONSTRAINT fk_doctor FOREIGN KEY (doctor_id)REFERENCES DOCTOR(doctor_id));
